# Redrob Candidate Ranker

This repository contains a candidate ranking pipeline for the Redrob challenge and is prepared for deployment as a Hugging Face Space.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
